
let arr = []
let count = 0

function Tickets(elementID){
     background(elementID)
     
}

function banner(){ 
    Hide('headers')
    Hide('footers')
    showElementById('main-body')
    count = 0;
    arr = []
    setelement('greenline',40)
    setelement('seat', 0)
}
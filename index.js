
let arr = []
let count = 0

function Tickets(){
     console.log(seat.classList)
}

function banner(){ 
    Hide('headers')
    Hide('footers')
    showElementById('main-body')
    count = 0;
    arr = []
    setelement('greenline',40)
    setelement('seat',0)
    Tickets()
}